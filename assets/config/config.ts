const API_URL: string = "http://localhost:8080/api";

export const AUTH_URI: string = `${API_URL}/authentication_token`;

export const USERS_URI: string = `${API_URL}/users`;

export const CUSTOMERS_URI: string = `${API_URL}/customers`;

export const INVOICES_URI: string = `${API_URL}/invoices`;